/** @type {import('next').NextConfig} */
const nextConfig = {
  // Portal subdomain config — update for production deployment
  // Domain: portal.vantagestrat.co
  experimental: {
    // serverActions enabled by default in Next 14
  },
};

export default nextConfig;
